[package]
name = "haltchain-policy"
version = "0.1.0"
edition = "2024"
description = "Aggregate circuit breakers and policy domains"
license = "MIT"
repository = "https://github.com/Haltchain/HaltChain"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
