[package]
name = "haltchain-ops"
version = "0.1.0"
edition = "2024"
description = "Sampling, SLA monitoring, and operational metrics"
license = "MIT"
repository = "https://github.com/Haltchain/HaltChain"

[dependencies]
serde = { workspace = true }
rand = "0.8"

[features]
default = []
experimental = []

[dev-dependencies]
